type TotalType = {
  title: string;
  detail: number;
};

type FomatationType = {
  title: string;
  list: string[];
};

type LevelType = {
  title: string;
  value: number;
};

type FieldCardsType = {
  top: string[];
  middle: string[];
  bottom: string[];
};

export type { TotalType, FomatationType, LevelType, FieldCardsType };
